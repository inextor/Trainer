#!/bin/sh
# Bump the app version before each commit.
# Install with: scripts/install-hooks.sh
node scripts/bump-version.js || exit 1
git add package.json webapp/index.html webapp/calendar.html webapp/day.html
